# Models that do not survive a DXF export round trip yet #

Same semantics as the STEP and IGES `-export-future` folders, for the `DxfExportFuture` fixture
(`[Explicit]`). Baselines describe the wanted result; a green model has been fixed — move it and
its `.exported.txt` into `models\DXFfile-export\`. Say in a sidecar comment whether the export or
the re-import is the broken half. Use `MX_DXFEXPORT_REGEN_DRYRUN=1` to look without writing.

## What is in here ##

Nothing yet.

See `DXF-tests-guide.md` at the repository root.
