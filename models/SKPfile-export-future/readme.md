# Models that do not survive a SketchUp export round trip yet #

Same semantics as the other `-future` folders, for the `SkpExportFuture` fixture (`[Explicit]`).
Baselines state the wanted result, not the current one; a model that comes up green has been
fixed - move it and its sidecar into the verified folder. Use `MX_SKPEXPORT_REGEN_DRYRUN=1` to look without writing.

## What is in here ##

Nothing yet.

See `SKP-tests-guide.md` at the repository root.
