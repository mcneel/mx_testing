# Models that do not survive a 3MF export round trip yet #

Same semantics as the other `-future` folders, for the `TmfExportFuture` fixture (`[Explicit]`).
Baselines state the wanted result, not the current one; a model that comes up green has been
fixed - move it and its sidecar into the verified folder. Use `MX_TMFEXPORT_REGEN_DRYRUN=1` to look without writing.

## What is in here ##

Nothing yet.

See `3MF-tests-guide.md` at the repository root.
