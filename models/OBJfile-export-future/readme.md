# Models that do not survive an OBJ export round trip yet #

Same semantics as the other `-future` folders, for the `ObjExportFuture` fixture (`[Explicit]`).
Baselines state the wanted result; a green model has been fixed - move it and its sidecar into
the verified folder. Use `MX_OBJEXPORT_REGEN_DRYRUN=1` to look without writing.

## What is in here ##

Nothing yet.

See `OBJ-tests-guide.md` at the repository root.
