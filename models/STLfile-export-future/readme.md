# Models that do not survive an STL export round trip yet #

Same semantics as the other `-future` folders, for the `StlExportFuture` fixture (`[Explicit]`).
Baselines state the wanted result; a green model has been fixed - move it and its sidecar into
the verified folder. Use `MX_STLEXPORT_REGEN_DRYRUN=1` to look without writing.

## What is in here ##

Nothing yet.

See `STL-tests-guide.md` at the repository root.
